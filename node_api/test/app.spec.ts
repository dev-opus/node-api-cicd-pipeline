import { app } from '../src/app';
import { expect, describe, beforeAll, afterAll, it } from 'vitest';

describe('Subnet Culator Test Suite', () => {
  it('correctly evaluates a properly formatted cidr address');

  it(
    'returns a validation error response for an improperly formatted cidr address'
  );
});
